<%- include("partials/header.ejs") %>

    <div class="container"> 
        <div class="maincontent">
            <div class="homepage">
                <h1>Your Very Own Blogging Library</h1>
                <a href="/create"><button id="createbtn" class="btn btn-dark rounded-pill px-3">Create New Blog</button></a>   
            </div>
            <% Content.forEach((post)=> {%>
                <div class="homepage posts">
                    <div>
                        <h3><a href="posts/<%=post.id%>"><%= post.title %></a></h3>
                        <p><%= post.genre %></p>
                    </div>
                    <div class="homebtns">
                        <form action="/posts/edit/<%= post.id %>" method="get">
                            <a href="posts/edit/<%= post.id %>"><button type="submit" class="btn btn-dark rounded-pill px-3">Edit</button></a>
                        </form>
                        <form action="/posts/delete/<%= post.id %>" method="post">
                            <button type="submit" class="btn btn-dark rounded-pill px-3">Delete</button>
                        </form>
                    </div>
                </div>
            <% }); %>
            
        </div> 
    </div>   
    <div>
        <%- include("partials/footer.ejs") %>
    </div>
